<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.ud08t00.MainActivity">  <!-- Corregido el context a la ruta correcta -->

    <!-- El CardView con el SearchView se mantiene igual, está bien estructurado -->
    <androidx.cardview.widget.CardView
        android:id="@+id/materialCardView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <SearchView
            android:id="@+id/searchView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:iconifiedByDefault="false"
            android:queryBackground="@android:color/transparent"
            android:queryHint="Buscar libro..." /> <!-- Pista de búsqueda actualizada -->
    </androidx.cardview.widget.CardView>

    <!--
    CORRECCIÓN 1: SwipeRefreshLayout
    El SwipeRefreshLayout debe tener un único hijo directo. El RecyclerView debe
    estar DENTRO de él. Las restricciones se aplican al SwipeRefreshLayout.
    -->
    <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
        android:id="@+id/srlDatos"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/materialCardView">

        <!--
        CORRECCIÓN 2: RecyclerView
        - Se cambia el ID a 'rvLibros' para que coincida con MainActivity.kt.
        - Los atributos layout_width y layout_height deben ser 'match_parent'
          para que ocupen todo el espacio que les da el SwipeRefreshLayout.
        -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rvLibros"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="8dp"
            android:clipToPadding="false"
            tools:listitem="@layout/item_libro" /> <!-- Aseguramos que la vista previa use el layout correcto -->

    </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
